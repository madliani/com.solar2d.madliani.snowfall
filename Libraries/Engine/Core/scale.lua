---@param x integer
---@param y integer
local Scale = function(x, y)
    ---@class Scale
    ---@field x integer
    ---@field y integer
    return {
        x = x,
        y = y,
    }
end

return Scale
